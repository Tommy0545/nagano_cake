<h2>商品一覧（全<%= @items.count %>件）</h2>

<% @items.each do |item| %>
      <%= link_to item_path(item.id) do %>
        <%= image_tag item.get_image(100,100), class:"m-0.5" %>
      <% end %>
        <%= item.name %>
        <%= item.price %>円
  <% end %>
      
<%= paginate @items_page %>
